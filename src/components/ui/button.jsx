import React from "react";
import { cn } from "@/lib/utils";
export const Button=React.forwardRef(function Button({className,variant="default",type="button",...props},ref){const variants={default:"bg-primary text-primary-foreground hover:opacity-90",outline:"border border-border bg-card text-foreground hover:bg-muted/60",ghost:"bg-transparent hover:bg-muted/60"};return <button ref={ref} type={type} className={cn("inline-flex items-center justify-center gap-2 rounded-xl px-4 py-2 text-sm font-semibold transition-colors disabled:pointer-events-none disabled:opacity-50 focus:outline-none focus:ring-2 focus:ring-primary/30",variants[variant]||variants.default,className)} {...props}/>});
