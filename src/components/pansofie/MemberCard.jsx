import React from "react";
import { MapPin } from "lucide-react";
import PathBadge from "@/components/pansofie/PathBadge";
export default function MemberCard({member}){const initials=member.name.split(" ").slice(0,2).map((n)=>n[0]).join("");return <div className="card-soft p-5"><div className="flex items-start gap-3"><div className="h-11 w-11 rounded-2xl bg-primary/10 text-primary flex items-center justify-center font-semibold shrink-0">{initials}</div><div className="min-w-0 flex-1"><h3 className="font-semibold font-heading truncate">{member.name}</h3><p className="text-xs text-muted-foreground mt-0.5">{member.role}</p><p className="text-xs text-muted-foreground mt-1 inline-flex items-center gap-1"><MapPin size={12}/>{member.location}</p></div></div><div className="flex flex-wrap gap-1.5 mt-4">{(member.paths||[]).slice(0,4).map((p)=><PathBadge key={p} pathId={p} size="xs" showName={false}/>)}</div>{member.availability&&<p className="text-xs text-muted-foreground mt-4">{member.availability}</p>}</div>}
