import React from "react";
import { Eye, Pencil, Trash2 } from "lucide-react";

export default function AdminListPage({ title, subtitle, columns, rows, extra }) {
  return <div><div className="mb-6"><h1 className="text-2xl font-semibold font-heading">{title}</h1>{subtitle && <p className="text-muted-foreground mt-1.5 text-sm">{subtitle}</p>}</div>{extra}<div className="card-soft overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-muted/50 text-muted-foreground text-xs uppercase tracking-wide"><tr>{columns.map((c)=><th key={c.key} className="text-left font-medium px-5 py-3 whitespace-nowrap">{c.label}</th>)}<th className="text-right font-medium px-5 py-3">Akce</th></tr></thead><tbody>{rows.map((row)=><tr key={row.id} className="border-t border-border/60 hover:bg-muted/30">{columns.map((c)=><td key={c.key} className="px-5 py-3.5 whitespace-nowrap">{c.render ? c.render(row) : row[c.key]}</td>)}<td className="px-5 py-3.5 text-right"><div className="inline-flex gap-1"><button className="h-8 w-8 rounded-lg hover:bg-muted flex items-center justify-center text-muted-foreground"><Eye size={15}/></button><button className="h-8 w-8 rounded-lg hover:bg-muted flex items-center justify-center text-muted-foreground"><Pencil size={15}/></button><button className="h-8 w-8 rounded-lg hover:bg-muted flex items-center justify-center text-destructive"><Trash2 size={15}/></button></div></td></tr>)}</tbody></table></div></div></div>;
}
