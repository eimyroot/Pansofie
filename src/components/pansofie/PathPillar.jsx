import React from "react";
export default function PathPillar({path}){const Icon=path.icon;return <article className="card-soft p-6 group transition-all duration-300 hover:-translate-y-1"><div className="h-12 w-12 rounded-2xl flex items-center justify-center mb-4 transition-transform group-hover:scale-110" style={{backgroundColor:`${path.color}18`,color:path.color}}><Icon size={24} strokeWidth={2}/></div><h3 className="text-xl font-semibold font-heading">{path.name}</h3><p className="text-sm text-muted-foreground mt-2 leading-relaxed">{path.develops}</p><div className="flex flex-wrap gap-1.5 mt-4">{path.areas.map((area)=><span key={area} className="chip bg-muted text-muted-foreground">{area}</span>)}</div></article>}
