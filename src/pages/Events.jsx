import React from "react";
import { Calendar, MapPin, Users } from "lucide-react";
import { EVENTS } from "@/lib/pansofieData";
import LabBadge from "@/components/pansofie/LabBadge";
export default function Events(){return <div className="px-5 sm:px-8 lg:px-12 py-8 max-w-7xl mx-auto"><div className="mb-8"><h1 className="text-2xl sm:text-3xl font-semibold font-heading">Události</h1><p className="text-muted-foreground mt-1.5">Komunitní akce, výzvy a setkání ve tvém okolí.</p></div><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">{EVENTS.map((e)=><div key={e.id} className="card-soft p-6 flex flex-col gap-4"><div className="flex items-center justify-between"><LabBadge labId={e.lab}/><span className="text-xs text-muted-foreground">{e.attendees}/{e.capacity}</span></div><div><h3 className="text-lg font-semibold font-heading leading-snug">{e.name}</h3><p className="text-sm text-muted-foreground mt-1.5">{e.desc}</p></div><div className="flex flex-col gap-1.5 text-sm text-muted-foreground mt-auto"><span className="inline-flex items-center gap-1.5"><Calendar size={14}/>{e.date} · {e.time}</span><span className="inline-flex items-center gap-1.5"><MapPin size={14}/>{e.location}</span><span className="inline-flex items-center gap-1.5"><Users size={14}/>{e.attendees} přihlášených</span></div><button className="mt-1 px-4 py-2.5 bg-primary text-primary-foreground rounded-xl font-semibold text-sm">Přihlásit se</button></div>)}</div></div>}
