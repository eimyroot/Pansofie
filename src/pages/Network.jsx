import React,{useState} from "react";
import { Search,SlidersHorizontal,Sparkles } from "lucide-react";
import { MEMBERS,PROJECTS,PATHS } from "@/lib/pansofieData";
import MemberCard from "@/components/pansofie/MemberCard";
import ProjectCard from "@/components/pansofie/ProjectCard";
export default function Network(){const[tab,setTab]=useState("people");const[activePath,setActivePath]=useState(null);const filteredMembers=activePath?MEMBERS.filter((m)=>m.paths.includes(activePath)):MEMBERS;const filteredProjects=activePath?PROJECTS.filter((p)=>p.paths.includes(activePath)):PROJECTS;return <div className="px-5 sm:px-8 lg:px-12 py-8 max-w-7xl mx-auto"><div className="mb-8"><h1 className="text-2xl sm:text-3xl font-semibold font-heading">Síť</h1><p className="text-muted-foreground mt-1.5 max-w-3xl">Najdi lidi, projekty, týmy, mentory a akce. Pansofie není feed ani popularita — je to hledání smysluplné spolupráce.</p></div><div className="card-soft p-5 mb-6 bg-primary/[0.03] border-primary/20 flex items-start gap-3"><Sparkles size={20} className="text-primary shrink-0 mt-0.5"/><p className="text-sm"><span className="font-semibold">Návrh pro tebe:</span> Projekt ve tvém okolí hledá člověka na grafiku a finance a rozvíjí Tvořivost + Samostatnost + Přínos.</p></div><div className="flex flex-col sm:flex-row gap-3 mb-5"><div className="relative flex-1"><Search size={18} className="absolute left-4 top-1/2 -translate-y-1/2 text-muted-foreground"/><input placeholder="Hledat lidi, projekty, týmy…" className="w-full pl-11 pr-4 py-3 bg-card border border-border rounded-2xl text-sm"/></div><button className="px-4 py-3 bg-card border border-border rounded-2xl text-sm font-medium inline-flex items-center gap-2"><SlidersHorizontal size={16}/>Filtry</button></div><div className="flex flex-wrap gap-2 mb-6"><button onClick={()=>setActivePath(null)} className={`chip ${!activePath?"bg-primary text-primary-foreground":"bg-muted text-muted-foreground"}`}>Vše</button>{PATHS.map((p)=>{const active=activePath===p.id;return <button key={p.id} onClick={()=>setActivePath(active?null:p.id)} className="chip" style={active?{backgroundColor:p.color,color:"#fff"}:{backgroundColor:`${p.color}14`,color:p.color}}>{p.name}</button>})}</div><div className="flex gap-1 mb-6 border-b border-border"><button onClick={()=>setTab("people")} className={`px-4 py-2.5 text-sm font-medium border-b-2 ${tab==="people"?"border-primary text-primary":"border-transparent text-muted-foreground"}`}>Lidé ({filteredMembers.length})</button><button onClick={()=>setTab("projects")} className={`px-4 py-2.5 text-sm font-medium border-b-2 ${tab==="projects"?"border-primary text-primary":"border-transparent text-muted-foreground"}`}>Projekty ({filteredProjects.length})</button></div>{tab==="people"?<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">{filteredMembers.map((m)=><MemberCard key={m.id} member={m}/>)}</div>:<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">{filteredProjects.map((p)=><ProjectCard key={p.id} project={p}/>)}</div>}</div>}
