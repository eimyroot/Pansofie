import React from "react";
import { CURRENT_USER,getPath } from "@/lib/pansofieData";
export default function Rozvoj(){const u=CURRENT_USER;const total=u.paths.reduce((s,p)=>s+p.experiences,0);const max=Math.max(...u.paths.map((p)=>p.experiences));return <div className="px-5 sm:px-8 lg:px-12 py-8 max-w-7xl mx-auto"><div className="mb-8"><h1 className="text-2xl sm:text-3xl font-semibold font-heading">Můj rozvoj</h1><p className="text-muted-foreground mt-1.5 max-w-3xl">Doložený rozvoj, ne hodnocení „hodnoty člověka“. Počítáme zkušenosti, mise, projekty a oblasti, které jsi vyzkoušel.</p></div><div className="card-soft p-6 mb-6"><p className="text-sm text-muted-foreground">Celkem zkušeností napříč 7 cestami</p><p className="text-4xl font-semibold font-display mt-1">{total}</p></div><div className="flex flex-col gap-4">{u.paths.map((up)=>{const path=getPath(up.id);const Icon=path.icon;const pct=Math.round(up.experiences/max*100);return <div key={up.id} className="card-soft p-5"><div className="flex items-center gap-4 mb-3"><div className="h-11 w-11 rounded-2xl flex items-center justify-center shrink-0" style={{backgroundColor:`${path.color}18`,color:path.color}}><Icon size={22}/></div><div className="flex-1"><p className="font-semibold font-heading">{path.name}</p><p className="text-xs text-muted-foreground">{up.experiences} zkušeností · {up.missions} dokončených misí</p></div></div><div className="h-2 rounded-full bg-muted overflow-hidden"><div className="h-full rounded-full" style={{width:`${pct}%`,backgroundColor:path.color}}/></div></div>})}</div></div>}
