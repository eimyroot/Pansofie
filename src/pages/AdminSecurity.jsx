import React,{useState} from "react";
import { ShieldCheck } from "lucide-react";
import { Switch } from "@/components/ui/switch";
const SETTINGS=[{title:"Dvoufázové ověření",desc:"Vyžadovat 2FA pro administrátorské účty.",on:true},{title:"Ověření identity mentorů",desc:"Mentoři projdou ověřením před schvalováním mise.",on:true},{title:"Soukromí profilů dětí",desc:"Profily dětí jsou viditelné jen pro rodiče a učitele.",on:true},{title:"Moderace obsahu",desc:"Nové projekty a mise projdou schválením.",on:true},{title:"Export dat (GDPR)",desc:"Uživatelé mohou požádat o export svých dat.",on:false},{title:"Mazání neaktivních účtů",desc:"Po 24 měsících neaktivity.",on:false}];
export default function AdminSecurity(){const[state,setState]=useState(SETTINGS.map((s)=>s.on));return <div><div className="mb-6"><h1 className="text-2xl font-semibold font-heading">Bezpečnost</h1><p className="text-muted-foreground mt-1.5 text-sm">Ochrana uživatelů, dat a soukromí.</p></div><div className="flex flex-col gap-2 max-w-2xl">{SETTINGS.map((s,i)=><div key={s.title} className="card-soft p-5 flex items-center justify-between gap-4"><div className="flex items-start gap-3"><ShieldCheck size={18} className="text-primary mt-0.5 shrink-0"/><div><p className="font-medium text-sm">{s.title}</p><p className="text-xs text-muted-foreground mt-0.5">{s.desc}</p></div></div><Switch checked={state[i]} onCheckedChange={(v)=>setState((prev)=>prev.map((p,idx)=>idx===i?v:p))}/></div>)}</div></div>}
