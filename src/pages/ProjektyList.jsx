import React,{useState} from "react";
import { PROJECTS,LABS } from "@/lib/pansofieData";
import ProjectCard from "@/components/pansofie/ProjectCard";
export default function ProjektyList(){const[activeLab,setActiveLab]=useState(null);const filtered=activeLab?PROJECTS.filter((p)=>p.lab===activeLab):PROJECTS;return <div className="px-5 sm:px-8 lg:px-12 py-8 max-w-7xl mx-auto"><div className="mb-8"><h1 className="text-2xl sm:text-3xl font-semibold font-heading">Projekty</h1><p className="text-muted-foreground mt-1.5">Větší celky než mise. Připoj se k týmu nebo založ vlastní.</p></div><div className="flex flex-wrap gap-2 mb-6"><button onClick={()=>setActiveLab(null)} className={`chip ${!activeLab?"bg-primary text-primary-foreground":"bg-muted text-muted-foreground"}`}>Vše</button>{LABS.map((l)=><button key={l.id} onClick={()=>setActiveLab(activeLab===l.id?null:l.id)} className="chip" style={activeLab===l.id?{backgroundColor:l.color,color:"#fff"}:{backgroundColor:`${l.color}14`,color:l.color}}>{l.name}</button>)}</div><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">{filtered.map((p)=><ProjectCard key={p.id} project={p}/>)}</div></div>}
