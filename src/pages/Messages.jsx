import React,{useState} from "react";
import { Send,Search } from "lucide-react";
import { CONVERSATIONS } from "@/lib/pansofieData";
export default function Messages(){const[active,setActive]=useState(CONVERSATIONS[0]);return <div className="px-5 sm:px-8 lg:px-12 py-8 max-w-7xl mx-auto"><div className="mb-8"><h1 className="text-2xl sm:text-3xl font-semibold font-heading">Zprávy</h1><p className="text-muted-foreground mt-1.5">Spojení s lidmi a týmy na společné mise.</p></div><div className="card-soft overflow-hidden grid grid-cols-1 sm:grid-cols-[300px_1fr] h-[70vh]"><div className="border-b sm:border-b-0 sm:border-r border-border/60 overflow-y-auto"><div className="p-3 border-b border-border/60 sticky top-0 bg-card"><div className="relative"><Search size={15} className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground"/><input placeholder="Hledat…" className="w-full pl-9 pr-3 py-2 bg-muted/50 rounded-lg text-sm focus:outline-none"/></div></div>{CONVERSATIONS.map((c)=><button key={c.id} onClick={()=>setActive(c)} className={`w-full text-left p-4 border-b border-border/40 flex gap-3 ${active.id===c.id?"bg-primary/[0.06]":"hover:bg-muted/40"}`}><div className="h-10 w-10 rounded-xl bg-primary/10 text-primary flex items-center justify-center font-semibold shrink-0">{c.name[0]}</div><div className="flex-1 min-w-0"><div className="flex items-center justify-between gap-2"><p className="font-medium text-sm truncate">{c.name}</p><span className="text-[11px] text-muted-foreground shrink-0">{c.time}</span></div><p className="text-xs text-muted-foreground truncate mt-0.5">{c.last}</p></div>{c.unread>0&&<span className="h-5 w-5 rounded-full bg-primary text-primary-foreground text-[11px] font-semibold flex items-center justify-center shrink-0">{c.unread}</span>}</button>)}</div><div className="flex flex-col min-h-0"><div className="p-4 border-b border-border/60"><p className="font-semibold font-heading">{active.name}</p><p className="text-xs text-muted-foreground">{active.role}</p></div><div className="flex-1 overflow-y-auto p-5 flex flex-col gap-3"><div className="max-w-[75%] bg-muted/60 rounded-2xl rounded-tl-sm px-4 py-2.5 text-sm">{active.last}</div><div className="max-w-[75%] self-end bg-primary text-primary-foreground rounded-2xl rounded-tr-sm px-4 py-2.5 text-sm">Díky! Určitě se spojíme. Kdy se ti to hodí?</div><div className="max-w-[75%] bg-muted/60 rounded-2xl rounded-tl-sm px-4 py-2.5 text-sm">Co tak ve čtvrtek od 18:00? Můžeme si ukázat aktuální mise.</div></div><div className="p-4 border-t border-border/60 flex gap-2"><input placeholder="Napiš zprávu…" className="flex-1 px-4 py-2.5 bg-muted/50 rounded-xl text-sm focus:outline-none focus:ring-2 focus:ring-primary/30"/><button className="h-11 w-11 rounded-xl bg-primary text-primary-foreground flex items-center justify-center"><Send size={18}/></button></div></div></div></div>}
